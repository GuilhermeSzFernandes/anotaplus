<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <ImageButton
        android:id="@+id/btn_mes_anterior"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/desc_mes_anterior"
        android:padding="6dp"
        android:src="@drawable/ic_chevron_left" />

    <TextView
        android:id="@+id/text_mes"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center"
        android:fontFamily="monospace"
        android:textColor="@color/ink"
        android:textSize="12sp"
        android:letterSpacing="0.04"
        tools:text="JULHO 2026" />

    <ImageButton
        android:id="@+id/btn_mes_proximo"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/desc_mes_proximo"
        android:padding="6dp"
        android:src="@drawable/ic_chevron_right" />

</LinearLayout>
